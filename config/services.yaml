# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    attachments_directory: '%kernel.project_dir%/public/uploads/attachments'
    app.loan.default_loan_days: 21
    app.loan.renewal_days: 14
    app.loan.max_renewals: 3
    app.loan.daily_penalty_rate: 0.50
    app.loan.daily_late_fee_rate: 0.50
    mailer.sender_email: '%env(MAILER_SENDER_EMAIL)%'
    mailer.sender_name: '%env(MAILER_SENDER_NAME)%'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name

    
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    App\Service\GoogleBooksService:
        arguments:
            $googleBooksApiKey: '%env(GOOGLE_BOOKS_API_KEY)%'
    App\Service\IAService:
        arguments:
            $openAiApiKey: '%env(OPENAI_API_KEY_waed)%'
            # ✅ NOUVEAU SERVICE IA (Pollinations) - SANS CLÉ API !
    App\Service\PollinationsService:
        arguments:
            $httpClient: '@http_client'
            $journalRepository: '@App\Repository\JournalLectureRepository'
            $defiRepository: '@App\Repository\DefiPersonelRepository'
            $cache: '@cache.app'
    # --- File Upload Service Configuration ---
    App\Service\FileUploadService:
        arguments:
            $attachmentsDirectory: '%attachments_directory%'

    App\Service\LoanService:
        arguments:
            $defaultLoanDays: '%app.loan.default_loan_days%'
            $renewalDays: '%app.loan.renewal_days%'
            $maxRenewals: '%app.loan.max_renewals%'
            $dailyPenaltyRate: '%app.loan.daily_penalty_rate%'

    App\Service\RenewalService:
        arguments:
            $renewalDays: '%app.loan.renewal_days%'
            $maxRenewals: '%app.loan.max_renewals%'

    App\Command\AccrueDailyLateFeesCommand:
        arguments:
            $dailyLateFeeRate: '%app.loan.daily_late_fee_rate%'

    App\Service\MailerService:
        arguments:
            $senderEmail: '%mailer.sender_email%'
            $senderName: '%mailer.sender_name%'

    # Commande d'export
    App\Command\ExportInteractionsCommand:
        arguments:
            $projectDir: '%kernel.project_dir%'
    
    # Service IA
    App\Service\AIRecommendationService:
        arguments:
            $aiServiceUrl: '%env(AI_SERVICE_URL)%'
    
    App\Service\ClubSimilarityService:
        arguments:
            $huggingfaceToken: '%env(HUGGINGFACE_TOKEN)%'