{% extends 'base.html.twig' %}
{% block title %}Nouvel auteur{% endblock %}
{% block body %}
<div class="container-fluid p-4">
    <h1 class="h3 mb-4">Nouvel auteur</h1>
    <div class="card border-0 shadow-sm" style="max-width: 640px;">
        <div class="card-body">
            {{ include('catalog/backoffice/author/_form.html.twig', { button_label: 'Créer' }) }}
            <a href="{{ path(area ~ '_author_index') }}" class="btn btn-link text-muted mt-2">Retour à la liste</a>
        </div>
    </div>
</div>
{% endblock %}
